
import { Navigate } from 'react-router-dom';

export default function Signup() {
  // Redirect to the signup page (assuming it's named differently or in a different path)
  return <Navigate to="/signup" replace />;
}
