interface CertificationProps {
    title: string;
    validUntil: string;
    iconUrl: string;
    alt: string;
}
export default function CertificationCard({ title, validUntil, iconUrl, alt }: CertificationProps): import("react").JSX.Element;
export {};
