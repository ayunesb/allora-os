interface LogsPanelProps {
  maxLogs?: number;
  showFilters?: boolean;
  tenantId?: string;
}
export declare function LogsPanel({
  maxLogs,
  showFilters,
  tenantId,
}: LogsPanelProps): JSX.Element;
export {};
