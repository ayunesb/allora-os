import { Communication } from "@/hooks/communications";
interface UpcomingCommunicationsProps {
    communications: Communication[];
    isLoading: boolean;
}
export default function UpcomingCommunications({ communications, isLoading }: UpcomingCommunicationsProps): import("react").JSX.Element;
export {};
