/**
 * Validates a URL string
 */
export declare function validateMediaUrl(url: string): boolean;
/**
 * Validates an array of media URLs
 */
export declare function validateMediaUrls(urls: string[]): boolean;
