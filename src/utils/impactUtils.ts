import { Campaign } from "@/types/fixed";

export function calculateImpact(campaign: Campaign): number {
  // Example logic for calculating impact
  return campaign.budget * 1.2; // Placeholder logic
}
