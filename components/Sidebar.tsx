import React from 'react'; // Fix default import

const Sidebar = () => {
  return (
    <div className="bg-gradient-to-b from-[var(--sidebar-bg-start)] to-[var(--sidebar-bg-end)] text-foreground border-r border-[var(--border)] backdrop-blur-md">
      {/* Sidebar content */}
    </div>
  );
};

export default Sidebar;
